# ============================================================
# Framework Configuration
# Override any property via system property: -Dkey=value
# ============================================================

# Application
base.url=https://www.saucedemo.com

# Browser: chrome | firefox
browser=chrome

# Headless mode (true for CI/CD)
headless=false

# Timeouts (seconds)
implicit.wait=10
explicit.wait=15
page.load.timeout=30
